node_modules/
dist/
# Cached output of our own generators (src/tools/bake.mjs). Not an authored asset:
# deleted, the game regenerates it at runtime; stale, the build step re-bakes.
public/baked/
.DS_Store
.claude/worktrees/
# Repo-local scratchpad: ignore everything in it except its README. Capture
# output (`tmp/shots/`) and one-off probe scripts live here -- it reached 1.7 GB
# of lossless PNGs once, and none of it is authored.
tmp/*
!tmp/README.md
# Review notes and camera tuning written by the in-game dev suite
# (src/tools/vite-plugin-review.mjs). Authored observations, not build output --
# drained by hand or by an agent, then deleted.
.review/
