# Human review

Things that need **you**, not a lane. Everything else is `project/TASKS.md`.
Keep this short. Delete a line once it is decided.

- **Nobody has played this.** A human drove the car for a minute and found the steering mirrored — a bug 19 gates, 142 shots and both perf gates could not see. Assume there are more.
- **The head is the judge's #1 (worth 3.0 → 4.0) and is closed short of the bar.** Six passes. Do we ever go back?
- **Idle CPU is unfixed on a 60 Hz panel** — the 60 cap only helps 120 Hz. Next lever is `post.render`, 74–77% of the frame.
- **The corduroy: fund it or close it?** A world-wide heightfield change across five generators, and **no instrument in the repo measures directional statistics**. Priced as not worth it for one establishing shot.
- **`crestholm_inlet` — may a lay-by become a clearing?** The seat that lands is found and 45 m away; Ostium Gorge is a parking bay. Content call, nothing left to measure.
- **Lane 13's exit was priced against the wrong number, and the plan should say so.** The tab is **1 382 MB**, not the plan's 1 246 (that figure was an older run's `--type=renderer` row). Everything plan tasks 38-41 name is worth **~15 MB against a 582 MB gap**; lane 13 landed -42.1 MB of it, deterministic. The 582 MB lives in render targets (lane 15), the bake path's ~2x inflate peak (lane 14) and 213.5 MB of doubly-resident CPU geometry — all filed in `project/TASKS.md`. **Do we re-fund a memory lane against those three, or accept the tab where it lands and drop the <800 MB bullet from the DoD?**
- **Party bulk: CLOSED at 20 mm**, and from a measurement rather than a picture. Lane 2 built two probes that measure garment-minus-body radius over all (theta, t) in the bind pose: at clearance **zero** the worst case anywhere is **+8.8 mm** (jacket), so nothing was ever inside the skin unposed and all 30 mm was pose margin. 12 mm visibly fails (bare hips on Ignis, bare thigh on Noctis); **20 mm is clean on all four**, and Ignis and Noctis get a waist and a real sleeve back. Verified on idle/walk only — a combat pose is still unphotographed.
- **New art call: Ignis's coat is now Y 57, the lightest garment in the party.** Deliberate — it is what makes him legible at 4 m, and it took his coat-vs-trousers separation to **19/255** against a bar of 12 (8.6 as authored). First attempt at 0x3d3b46 rendered *periwinkle*, a near-neutral albedo under an all-sky key, so it is warm-biased to 0x3c3936. **Worth checking beside the FFXV plates.**
- **Two of the plan's Part C exits are unreachable as written, and both now need your ruling.** Lane 15 measured rather than argued. **Task 44's "idle < 30% of a core"** cannot come from `postfx/`: the chain after the scene draw is ~0.3 ms of a 6.7 ms frame, and `post.render` is 74-77% of the frame only because `renderer.render(scene, camera)` sits inside it. 30% needs 5 CPU ms/frame across all processes when the scene draw alone is 4.4 ms of main thread. **Task 45's "RT walk < 120 MB"** was set against a formula that reads 130 MB where the honest resident figure is **188.76 MB** (`bootprof`'s `sizeOfRt` ignores `samples`). Reaching 120 needs **MSAA 4→2 (8→4 at ultra) plus half-res GTAO** — both visible-quality calls on the "beautiful over fast" axis this project's BRIEF puts first. **Cut the quality, or restate both exits?**
- **Task 43's "≤25 MB to first frame" is unreachable by any lane, and the reason is structural.** Lane 14 built task 42's instrument first and then priced the exit off the live containers. `tex.bin.gz` is props 17.60 / dgn 6.78 / town 5.74 / sky 1.74 MB gz; `terrain.bin.gz` is h 11.98 / ctrl 8.34 / layerSurf 3.58 / layerAlbedo 2.97 / far 2.94 / layerDetail 1.73 / farCtrl 0.85 / hydro 0.78. **Everything except `dgn/*` and `texc` mip 0 is consumed before the first frame, because the first frame IS the end of `Game.init()`.** So the reachable floor is roughly **86.6 → 58 MB** (q16d on `h`/`far` −7.7, `dgn` deferral −6.8, `texc` mip 0 −14.4). Getting to 25 means changing *when the first frame happens* — i.e. `Game.ts`, which `BRIEF.md` rule 4 makes shared and which no lane owns. **Restate the exit at the measured floor, or fund an owner for a boot-order change in `Game.ts`?**
- **Plan task 47: one of the two VOID heads is now clear, and the other is a question about art direction rather than a defect.** **Prompto came off VOID** (61.2 → 58.7 against a ceiling of 60) once hair self-occlusion landed. **Noctis stays VOID at 95.1**, and four passes have now established why the standing diagnosis was wrong: `coverageAA` — the fix `facecheck.mts:160-167` names — moved him 96.3 → 96.2, and hiding the **entire groom and its cast shadow** on the same game build moves him only to 90.2. So the hair is worth 6 of the 96; at 8x the mark is a broad *soft* ramp of the head's own form and paint, and since `range` is p97−p03, softening cannot help it. **Either `facecheck` needs a third control candidate away from the malar, or you rule that a face half-covered by a soft fringe shadow at a 0.55 m closeup is the intended look.** Task 47 stays unlanded until you rule — deliberately, so the trunk never reds on an unsettled question.
- **The judged set now has five city/landmark rows and the plate library has one town plate.** `docs/reference/plates/` holds `town-daytime-altissia-01.jpg` and nothing else urban: no Lestallum, no Galdin Quay, no market square, no lit street at night, no coastal hotel. Lane 21 added `lest_market_day`, `lest_street_night`, `lest_overlook_disc`, `galdin_pier_sunset` and `galdin_angelgard` to `compare.mts` PAIRING and had to match three of them on *light and subject-kind* rather than on place — a daytime market square is judged against Altissia and a posed party plate; a lit square at night against Insomnia and a campfire. Scene-matching is the validity of the whole test (`compare.mts`'s own header says so), so **these three rows are the weakest arm of round 17 until you drop four or five real FFXV plates in**: Lestallum's market by day, Lestallum's street at night, Galdin Quay's pier at golden hour, and the Disc of Cauthess from a distance after dark. No lane can fetch them — BRIEF rule 1.
- **Combat difficulty is now well past the plan's bar, and nobody has played it.** Lane 11 met all four exits by measurement: median den duration **11.4 s → 23.8 s** (bar 18–30) and HP paid **3.2% → 25.2% median, 49.1% worst** against a bar of **15%**. Both perf gates certify (`perf` 163/163 shots, `gameplay` every segment ≥60 fps). The levers were `INCOMING_SCALE` 0.55→1.0, ten `maxEngaged` 3→4, hostile counts two deeper, and a real bug fix (pooled enemies were keeping scaled poise and compounding it on every respawn, so animals drifted toward unstaggerable the longer you stayed). **Every lever is measured; nothing here is guesswork — but a 49% worst-case den is a big felt change and the stack has never been played end to end. Play one den before this locks.**
- **`landmark_meteor` contains 9% of its own subject, and that is probably part of the blind verdict.** Measured with `probes/discview.mts`: a foreground ridge occludes everything below the Disc's crown, so a shot named for a 1.5 km impact landmark **cannot show a crater at any time of day**, and no art inside the meteor group can change it. The art round itself worked — the crater rim and apron had been **70–90 m underground since they were built** (median −70 m against `Terrain.heightAt`, now +3 m), and lit vein samples clearing every occluder went **0 → 104** from that stand. Exactly one place in the world sees the crater rim: the Disc overlook off the Cauthess highway, ~40 m above the parking, frac 0.82 — now shot as `disc_crater_night`. **Do we re-pose `landmark_meteor` there too, and accept that it re-baselines a judged frame?**

- **`landmark_meteor`: the answer to the line above is NO, and it is measured.** Lane 21 shot all four of `discview`'s best rings (east 3.0 km frac 0.42, west 2.4 km 0.43, north-east 2.0 km 0.41, two aim heights each) and read them beside the *current* shot captured through `shoot.mts`. **The current frame beats every one of them**: its crown is a dark faceted mass at ~28% of frame height with the blue fissures lit, over a sunlit ridge that gives it scale, while every 0.41–0.43 stand is 2–3 km out with aerial haze washing the mass to almost exactly the sky's own value — it reads as one more distant mountain. None of the six shows the crater either; only `n_disc` at eye 300 does, and that is `disc_crater_night`. **So 0.09 is a terrain-clearance number, not a picture-quality one**, and re-posing to a better one would make the shot worse and re-baseline a judged row to do it. Nothing here needs you any more — the line above can be deleted. Frames: `tmp/shots/l21-rm/MT_*.jpg`, `tmp/shots/l21-met/landmark_meteor.jpg`.
- **The Insomnia skyline stands *inside* the Adamantoise Graveyard, and it is a composition call, not a bug.** Lane 18 flagged that its bone country "now has a dead city on its horizon" and said it wanted a call above that lane. Lane 21's frames make it concrete and worse than "on the horizon": in `tmp/shots/l21-arc/GY_9.jpg` **two full-height blue glass office towers with window grids stand in the field beside the rib arches**, at full opacity with no aerial perspective, and at night (`MO_7.jpg`, the Mencemoor obelisks) the same LOD's emissive windows clip to **solid pure-white blocks**. It is visible from `adamantoise_graveyard`, `mencemoor_obelisks` and `peak_overlook`. **And it cannot be dodged, because the site is inside the city's footprint.** Standing on the bearing toward Insomnia and looking back — which the second-pass framings do — left the towers larger, not smaller, so `tmp/l21/mega.mts` measured it: from the graveyard camera the nearest `capital_mega_cityLit` instance is **776 m away and 636 m tall**, with a `capital_mega_lamp` at y 805–815. The POI pin at (3100, −2350) is **990 m from Insomnia at (2560, −3180)**; the pin it was moved from, (2600, −2800), was **382 m**. So "the empty quarter, farthest from everything" is a kilometre from the Crown City and always was, and no camera bearing hides a 600 m skyline at 800 m. **Either the graveyard moves out of Leide's north-east corner, or Insomnia's own draw distance / fog falloff hides it, or the bone country is meant to be in the Crown City's shadow.** One of those, and it is yours; until it is, `adamantoise_graveyard` and `graveyard_night` stay out of the corpus.
