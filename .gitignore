node_modules/
dist/
# Cached output of our own generators (tools/bake.mjs). Not an authored asset:
# deleted, the game regenerates it at runtime; stale, the build step re-bakes.
public/baked/
shots/
.DS_Store
.claude/worktrees/
# Review notes and camera tuning written by the in-game dev suite
# (tools/vite-plugin-review.mjs). Authored observations, not build output --
# drained by hand or by an agent, then deleted.
.review/
